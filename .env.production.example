# Backend runtime settings
NODE_ENV=production
HOST=0.0.0.0
PORT=5000

# Required in production for token signing
JWT_SECRET=replace-with-a-long-random-secret
JWT_EXPIRES_IN=8h

# Must match the public origin that serves the frontend through Caddy
FRONTEND_URL=http://92.5.68.152
