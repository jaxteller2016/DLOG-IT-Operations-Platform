{
  "name": "dlog-it-operations-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["frontend", "backend"],
  "scripts": {
    "dev": "concurrently \"node backend/src/server.js\" \"cd frontend && npm run dev -- --host 127.0.0.1 --port 4173 --strictPort\"",
    "test": "npm run test --workspace=backend",
    "build": "npm run build --workspace=frontend"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
